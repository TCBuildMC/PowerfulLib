dependencies {
    api project(":util")
}

publishing {
    publications {
        create("mavenEvent", MavenPublication) {
            artifactId = "event"
            from components.java
        }
    }
}
